VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-only — never prefix with VITE_ (that would bundle it into client JS)
GEMINI_API_KEY=
# Optional: tried if the primary key is rate-limited or fails
GEMINI_API_KEY_BACKUP=
